# ----- Database -----
DB_ROOT_PASSWORD=dealflow_root
DB_USER=dealflow
DB_PASSWORD=dealflow
DB_NAME=dealflow
DB_PORT_HOST=3307

# ----- JWT Secrets -----
JWT_SECRET=change-me-access
JWT_REFRESH_SECRET=change-me-refresh

# ----- n8n Configuration -----
N8N_HOST=localhost
N8N_PORT=5678
N8N_PROTOCOL=http
N8N_WEBHOOK_URL=http://localhost:5678/
N8N_ENCRYPTION_KEY=change-me-encryption-key
TZ=UTC
FX_API_URL=https://api.exchangerate-api.com/v4/latest/USD
FX_SCHEDULE_CRON=0 2 * * *

# ----- Frontend -----
FRONTEND_PORT=5173
VITE_API_URL=http://localhost:5678
VITE_APP_ENV=docker

# ----- Source Sync -----
APP_SOURCE_STRATEGY=git
APP_GIT_REPO=https://github.com/your-org/DealFlow.git
APP_GIT_REF=main

# ----- SMTP placeholders -----
SMTP_HOST=mailhog
SMTP_PORT=1025
SMTP_USER=
SMTP_PASS=
